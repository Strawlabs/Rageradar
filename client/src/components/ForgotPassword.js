import PasswordReset from './PasswordReset';

/**
 * ForgotPassword component
 * Re-exports PasswordReset so `/forgot-password` route maps cleanly to the password recovery screen.
 */
export default PasswordReset;
